name: Deploy Lambda

on:
  push:
    branches: [main, develop]
    paths:
      - 'src/lambdas/**'

env:
  AWS_REGION: ${{ vars.AWS_REGION }}

jobs:
  # ==========================================================================
  # ENVIRONMENT RESOLUTION — branch determines target environment
  #   develop → dev
  #   main    → prod
  # ==========================================================================
  resolve-env:
    name: Resolve Environment
    runs-on: ubuntu-latest
    outputs:
      env_name: ${{ steps.env.outputs.name }}
      role: ${{ steps.env.outputs.role }}
    steps:
      - name: Resolve environment
        id: env
        run: |
          if [ "${{ github.ref_name }}" = "main" ]; then
            echo "name=prod" >> "$GITHUB_OUTPUT"
            echo "role=${{ vars.AWS_ROLE_TO_ASSUME_PROD }}" >> "$GITHUB_OUTPUT"
          else
            echo "name=dev"  >> "$GITHUB_OUTPUT"
            echo "role=${{ vars.AWS_ROLE_TO_ASSUME_DEV }}" >> "$GITHUB_OUTPUT"
          fi

  deploy-video-upload:
    name: Deploy video-upload Lambda (${{ needs.resolve-env.outputs.env_name }})
    runs-on: ubuntu-latest
    needs: resolve-env
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ needs.resolve-env.outputs.role }}
          aws-region: ${{ env.AWS_REGION }}

      - uses: aws-actions/setup-sam@v2
        with:
          use-installer: true

      - name: Build
        run: sam build VideoUploadFunction --template template.yaml --use-container

      - name: Deploy
        uses: aws-actions/aws-lambda-deploy@v1
        with:
          function-name: lectureclip-${{ needs.resolve-env.outputs.env_name }}-video-upload
          code-artifacts-dir: .aws-sam/build/VideoUploadFunction
          handler: index.handler
          runtime: python3.13

  deploy-multipart-init:
    name: Deploy multipart-init Lambda (${{ needs.resolve-env.outputs.env_name }})
    runs-on: ubuntu-latest
    needs: resolve-env
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ needs.resolve-env.outputs.role }}
          aws-region: ${{ env.AWS_REGION }}

      - uses: aws-actions/setup-sam@v2
        with:
          use-installer: true

      - name: Build
        run: sam build MultipartInitFunction --template template.yaml --use-container

      - name: Deploy
        uses: aws-actions/aws-lambda-deploy@v1
        with:
          function-name: lectureclip-${{ needs.resolve-env.outputs.env_name }}-multipart-init
          code-artifacts-dir: .aws-sam/build/MultipartInitFunction
          handler: index.handler
          runtime: python3.13

  deploy-multipart-complete:
    name: Deploy multipart-complete Lambda (${{ needs.resolve-env.outputs.env_name }})
    runs-on: ubuntu-latest
    needs: resolve-env
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ needs.resolve-env.outputs.role }}
          aws-region: ${{ env.AWS_REGION }}

      - uses: aws-actions/setup-sam@v2
        with:
          use-installer: true

      - name: Build
        run: sam build MultipartCompleteFunction --template template.yaml --use-container

      - name: Deploy
        uses: aws-actions/aws-lambda-deploy@v1
        with:
          function-name: lectureclip-${{ needs.resolve-env.outputs.env_name }}-multipart-complete
          code-artifacts-dir: .aws-sam/build/MultipartCompleteFunction
          handler: index.handler
          runtime: python3.13

  deploy-s3-trigger:
    name: Deploy s3-trigger Lambda (${{ needs.resolve-env.outputs.env_name }})
    runs-on: ubuntu-latest
    needs: resolve-env
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ needs.resolve-env.outputs.role }}
          aws-region: ${{ env.AWS_REGION }}

      - uses: aws-actions/setup-sam@v2
        with:
          use-installer: true

      - name: Build
        run: sam build S3TriggerFunction --template template.yaml --use-container

      - name: Deploy
        uses: aws-actions/aws-lambda-deploy@v1
        with:
          function-name: lectureclip-${{ needs.resolve-env.outputs.env_name }}-s3-trigger
          code-artifacts-dir: .aws-sam/build/S3TriggerFunction
          handler: index.handler
          runtime: python3.13

  deploy-start-transcribe:
    name: Deploy start-transcribe Lambda (${{ needs.resolve-env.outputs.env_name }})
    runs-on: ubuntu-latest
    needs: resolve-env
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ needs.resolve-env.outputs.role }}
          aws-region: ${{ env.AWS_REGION }}

      - uses: aws-actions/setup-sam@v2
        with:
          use-installer: true

      - name: Build
        run: sam build StartTranscribeFunction --template template.yaml --use-container

      - name: Deploy
        uses: aws-actions/aws-lambda-deploy@v1
        with:
          function-name: lectureclip-${{ needs.resolve-env.outputs.env_name }}-start-transcribe
          code-artifacts-dir: .aws-sam/build/StartTranscribeFunction
          handler: index.handler
          runtime: python3.13

  deploy-process-transcribe:
    name: Deploy process-transcribe Lambda (${{ needs.resolve-env.outputs.env_name }})
    runs-on: ubuntu-latest
    needs: resolve-env
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ needs.resolve-env.outputs.role }}
          aws-region: ${{ env.AWS_REGION }}

      - uses: aws-actions/setup-sam@v2
        with:
          use-installer: true

      - name: Build
        run: sam build ProcessTranscribeFunction --template template.yaml --use-container

      - name: Deploy
        uses: aws-actions/aws-lambda-deploy@v1
        with:
          function-name: lectureclip-${{ needs.resolve-env.outputs.env_name }}-process-transcribe
          code-artifacts-dir: .aws-sam/build/ProcessTranscribeFunction
          handler: index.handler
          runtime: python3.13

  deploy-process-results:
    name: Deploy process-results Lambda (${{ needs.resolve-env.outputs.env_name }})
    runs-on: ubuntu-latest
    needs: resolve-env
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ needs.resolve-env.outputs.role }}
          aws-region: ${{ env.AWS_REGION }}

      - uses: aws-actions/setup-sam@v2
        with:
          use-installer: true

      - name: Build
        run: sam build ProcessResultsFunction --template template.yaml --use-container

      - name: Deploy
        uses: aws-actions/aws-lambda-deploy@v1
        with:
          function-name: lectureclip-${{ needs.resolve-env.outputs.env_name }}-process-results
          code-artifacts-dir: .aws-sam/build/ProcessResultsFunction
          handler: index.handler
          runtime: python3.13

  deploy-query-segments:
    name: Deploy query-segments Lambda (${{ needs.resolve-env.outputs.env_name }})
    runs-on: ubuntu-latest
    needs: resolve-env
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ needs.resolve-env.outputs.role }}
          aws-region: ${{ env.AWS_REGION }}

      - uses: aws-actions/setup-sam@v2
        with:
          use-installer: true

      - name: Build
        run: sam build QuerySegmentsFunction --template template.yaml --use-container

      - name: Deploy
        uses: aws-actions/aws-lambda-deploy@v1
        with:
          function-name: lectureclip-${{ needs.resolve-env.outputs.env_name }}-query-segments
          code-artifacts-dir: .aws-sam/build/QuerySegmentsFunction
          handler: index.handler
          runtime: python3.13

  deploy-db-migrate:
    name: Deploy db-migrate Lambda (${{ needs.resolve-env.outputs.env_name }})
    runs-on: ubuntu-latest
    needs: resolve-env
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ needs.resolve-env.outputs.role }}
          aws-region: ${{ env.AWS_REGION }}

      - uses: aws-actions/setup-sam@v2
        with:
          use-installer: true

      - name: Build
        run: sam build DbMigrateFunction --template template.yaml --use-container

      - name: Deploy
        uses: aws-actions/aws-lambda-deploy@v1
        with:
          function-name: lectureclip-${{ needs.resolve-env.outputs.env_name }}-db-migrate
          code-artifacts-dir: .aws-sam/build/DbMigrateFunction
          handler: index.handler
          runtime: python3.13

      - name: Run migration
        run: |
          aws lambda invoke \
            --function-name "lectureclip-${{ needs.resolve-env.outputs.env_name }}-db-migrate" \
            --invocation-type RequestResponse \
            --region "${{ env.AWS_REGION }}" \
            --output json \
            --payload '{}' \
            response.json > invoke_meta.json

          if grep -q '"FunctionError"' invoke_meta.json; then
            echo "db-migrate Lambda reported a function error — schema migration failed:"
            cat response.json
            exit 1
          fi

          echo "db-migrate response: $(cat response.json)"